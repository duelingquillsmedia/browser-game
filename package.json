{
  "name": "eridan",
  "private": true,
  "version": "0.1.0",
  "description": "Eridan — a browser-based, D&D SRD-inspired MMORPG",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/client",
    "build": "npm run build --workspace=packages/engine && npm run build --workspace=apps/client",
    "test": "npm run test --workspace=packages/engine",
    "lint": "npm run lint --workspace=apps/client"
  }
}
