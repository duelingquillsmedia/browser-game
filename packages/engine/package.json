{
  "name": "@eridan/engine",
  "version": "0.1.0",
  "private": true,
  "description": "SRD-inspired turn-based combat and character rules engine for Eridan",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^5.0.1"
  }
}
